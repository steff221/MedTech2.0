// Страница (Next.js): распоред — дел за матичен лекар (GP).
// GP schedule uses the same implementation as the surgeon portal.
// The WaitingRoom is keyed by doctorId so walk-ins and statuses
// are isolated per doctor automatically.
export { default } from "@/app/doctor/schedule/page";
